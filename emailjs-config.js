/* Public EmailJS values only — never put a private key here.
   Fill these three after creating the EmailJS account + template.
   Public key is meant for the browser. */
window.ND_FLOW_EMAILJS = {
  publicKey: "",
  serviceId: "",
  templateId: "",
};
